# Server-side URL of the FastAPI backend (used by Next.js API route proxies).
# NOT prefixed with NEXT_PUBLIC_ — never exposed to the browser.
API_BASE_URL=http://localhost:8006

# The FastAPI X-API-Key value (server-side only).
# Must match the API_KEY value in your root .env file.
API_KEY=your-api-key-here
